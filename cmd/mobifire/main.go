package main

import (
	_ "embed"
)

func main() {
	NewGame()
	/*mb := NewMultiBoard(11, 11, 8)

	center := container.New(layout.NewCenterLayout(), mb.container)

	w.SetContent(center)

	w.ShowAndRun()*/
}
